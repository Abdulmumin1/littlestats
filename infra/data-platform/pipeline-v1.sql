INSERT INTO littlestats_events_v1_sink
SELECT
  schema_version,
  event_id,
  site_id,
  event_time,
  event_type,
  visitor_id,
  visit_id,
  is_billable,
  identified_user_id,
  url_path,
  url_query,
  url_hash,
  page_title,
  referrer_domain,
  referrer_path,
  event_name,
  event_properties,
  browser,
  browser_version,
  os,
  os_version,
  device,
  screen,
  country,
  region,
  city,
  language,
  timezone,
  engagement_seconds,
  utm_source,
  utm_medium,
  utm_campaign,
  utm_content,
  utm_term,
  campaign_bucket
FROM littlestats_events_v1;
